import { CustomIconTintDirective } from './custom-icon-tint.directive';

describe('CustomIconTintDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomIconTintDirective();
    expect(directive).toBeTruthy();
  });
});
