import { ForFilterPipe } from './for-filter.pipe';

describe('ForFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ForFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
